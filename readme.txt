Some ansible tasks
    - debug
    - maven_artifact
    - s3_sync
    - cloudformation
    - lambda
    - set_fact, register

Supported parameters for args in shell modules
- stdin
- strip_empty_ends 
- argv
- _raw_params 
- chdir, warn, stdin_add_newline, executable, creates, _uses_shell, removes